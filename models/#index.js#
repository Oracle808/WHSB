import { User, Subject } from "./User.common.js";

var mongoose = require("mongoose");
mongoose.connect(process.env["DATABASE_CONNECTION"]);

mongoose.user = mongoose.model("User", User);
.subject = mongoose.model("Subject", Subject);

export { Database };

