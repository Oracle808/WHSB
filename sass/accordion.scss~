@import url(http://fonts.googleapis.com/css?family=Roboto+Condensed:400,300,700|Roboto+Slab:400,700);

@import "main";

body {
	background:whitesmoke;
}

h1, strong {
	//@include font-family(Lato);
	//@include font-smoothing(antialiased);
}

@mixin accordion($colour) {
	ul {
		list-style-type:none;
	}
	ul li {
		@include font-family("Roboto Condensed");

		position:relative;
		display:block;
		outline:none;
		min-height:24px;
		@include font-size(16px);
		@include font-weight(bold);

		//text-transform:uppercase;
		color:black;
		cursor:pointer;
	}
	ul li a {
		@include font-smoothing(antialiased);
		@include font-family("Roboto Slab");
		@include font-size(14px);
		@include font-weight(normal);

		background:darken($colour, 5%);
		color:white;

		padding-top:9px;
		padding-bottom:9px;
		padding-left:10px;
		padding-right:10px;

		display:inline-block;
		border-bottom:1px solid transparent;
		width:100%;
		min-height:36px;
	}
	ul li a:hover {
		background:darken($colour, 15%);
	}
	ul li a span {
		background:$white;
		color:darken($colour, 12.5%);
		float:right;
		display:inline-block;
		width:36px;
		height:17px;
		text-align:center;
		border-radius:30px;
	}
}

#AccordionDemo1 {
	@include accordion($viridescent);
	margin-top:20px;
}

#AccordionDemo2 {
	@include accordion($blue);
	margin-top:20px;
}

#AccordionDemo3 {
	@include accordion($purple);
	margin-top:20px;
}

#AccordionDemo4 {
	@include accordion($ruby);
	margin-top:20px;
}

#MenuMaster {
	@include width(3);
	@include margin-left(0.5);
	@include margin-right(0.5);

	@include width-xs(12);
	@include margin-left-xs(0);
	@include margin-right-xs(0);

	height:100%;
	overflow:scroll;
}

#ArticleMaster {
	margin-top:5px;
	@include width(8);
	height:100%;
	padding:10px;
}

#ArticleMaster h1 {
	@include font-family("Roboto Condensed");
}

#ArticleMaster p, #ArticleMaster table {
	@include font-family("Roboto Slab");
}

#ArticleMaster p i {

}

#Container {
	padding-bottom:20px;
	height:100%;
}